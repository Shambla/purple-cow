import styles from './Footer.module.scss';

/**
 * The footer of the page
 *
 * @returns {JSX.Element} the footer
 */
const Footer = () => {
  return <footer className={styles.footer}>Test Project from Bill Streckfus</footer>
};

export default Footer;